PORTNAME=	filterlog
PORTVERSION=	0.7
CATEGORIES=	sysutils
MASTER_SITES=	# empty
DISTFILES=	# none
EXTRACT_ONLY=	# empty

MAINTAINER=	nitish.patel@veritawall.com
COMMENT=	Parse pflog(4) output

LICENSE=	BSD3CLAUSE

do-extract:
	${CP} -r ${FILESDIR} ${WRKSRC}

.include <bsd.port.mk>
